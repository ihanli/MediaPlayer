KNOWN ISSUES:
+) Order in Bitmap corresponds to selection (not playlist) order. However, tracks are inserted in correct order.

+) Unable to move a track to the bottom (won't mark the spot). Move last track instead.

+) Can't move a (single) track if not marked.
